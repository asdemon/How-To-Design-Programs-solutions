#lang sicp
;the procedure is immediately run, it is necessary to keep the internal state coherent
;when the procedure is not immediately run, the internal state is not consistent, and the
; output can be wrong